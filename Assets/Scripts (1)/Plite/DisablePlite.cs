using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisablePlite : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private GameObject plite, pliteSprite;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if (PliteScripts.correctCode){
        //     plite.SetActive(false);
        //     pliteSprite.SetActive(true);
        // }
    }
}
